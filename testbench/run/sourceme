rm -rf .venv

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

tmppath=`readlink -m ../python`
export PYTHONPATH=${tmppath}:$PYTHONPATH

tmppath=`readlink -m ../../src/python`
export PYTHONPATH=${tmppath}:$PYTHONPATH
